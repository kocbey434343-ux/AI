# =============================================================================
# BINANCE TESTNET CONFIGURATION TEMPLATE
# =============================================================================
# Bu dosyayı .env dosyanıza kopyalayın ve testnet API anahtarlarınızı ekleyin
# 
# Testnet API anahtarları için:
# 1. https://testnet.binance.vision/ adresine gidin
# 2. GitHub ile login olun
# 3. API Key oluşturun (Spot Trading permissions)
# 4. Aşağıdaki anahtarları doldurun

# =============================================================================
# TESTNET API ANAHTARLARI (ZORUNLU)
# =============================================================================
# TESTNET API Key - Spot Trading permissions gerekli
BINANCE_API_KEY=your_testnet_api_key_here

# TESTNET Secret Key
BINANCE_API_SECRET=your_testnet_secret_key_here

# =============================================================================
# TESTNET MODE AYARLARI
# =============================================================================
# Testnet kullanımı (ZORUNLU: true olmalı)
USE_TESTNET=true

# Prod endpoint koruması (ZORUNLU: false olmalı testnet için)
ALLOW_PROD=false

# Offline mode (testnet kullanırken false olmalı)
OFFLINE_MODE=false

# =============================================================================
# TESTNET VERİ YOLLARI (Environment Isolation)
# =============================================================================
# Testnet için ayrı data dizinleri
DATA_PATH=./data/testnet
LOG_PATH=./data/testnet/logs
BACKUP_PATH=./backup/testnet
TRADES_DB_PATH=./data/testnet/trades.db

# Environment isolation aktif
ENV_ISOLATION=on

# =============================================================================
# TESTNET RİSK YÖNETİMİ AYARLARI
# =============================================================================
# Testnet için agresif risk parametreleri (gerçek para değil)
DEFAULT_RISK_PERCENT=5.0
DEFAULT_LEVERAGE=5
DEFAULT_MAX_POSITIONS=5
DEFAULT_MIN_VOLUME=1000

# Günlük risk limitleri (testnet için gevşek)
MAX_DAILY_LOSS_PCT=10.0
MAX_CONSECUTIVE_LOSSES=8

# =============================================================================
# TESTNET TRADING AYARLARI
# =============================================================================
# Testnet için optimize edilmiş signal thresholds
BUY_SIGNAL_THRESHOLD=70
SELL_SIGNAL_THRESHOLD=30
BUY_EXIT_THRESHOLD=65
SELL_EXIT_THRESHOLD=35

# =============================================================================
# TESTNET MALIYET MODELLEMESİ
# =============================================================================
# Testnet için daha yüksek maliyetler (gerçekçi simülasyon)
COMMISSION_PCT_PER_SIDE=1.0
SLIPPAGE_PCT_PER_SIDE=3.0
MAKER_COMMISSION_PCT_PER_SIDE=0.8
TAKER_COMMISSION_PCT_PER_SIDE=1.2

# =============================================================================
# LOG VE MONİTORİNG
# =============================================================================
# Testnet için verbose logging
LOG_LEVEL=DEBUG
STRUCTURED_LOG_ENABLED=true
STRUCTURED_LOG_VALIDATION=true

# =============================================================================
# GELİŞTİRİCİ AYARLARI
# =============================================================================
# Feature flags (testnet için experimental features aktif)
ENABLE_EXPERIMENTAL_FEATURES=true
ENABLE_ADVANCED_ANALYTICS=true
ENABLE_PERFORMANCE_ATTRIBUTION=true

# Debug modları
DEBUG_TRADING=true
DEBUG_API_CALLS=false
DEBUG_SIGNAL_GENERATION=false

# =============================================================================
# NOT: Testnet kullanırken gerçek para harcanmaz!
# =============================================================================